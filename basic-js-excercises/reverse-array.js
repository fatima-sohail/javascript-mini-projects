//Array Reversal. Write a function that takes an array as input and 
//returns a new array with the elements in reverse order.

let arr = [0, 1, 2]
function reverseArray(item){
    return item.reverse()
}

console.log(reverseArray(arr));