// import assert here
const assert = require('assert');

describe('+', () => {
  it('returns the sum of its arguments', () => {
    // Write assertion here
    //this function allows you to compare values and throw errors as needed using one function call
    assert.ok(3 + 4 === 7)
  });
});
